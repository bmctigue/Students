source 'https://github.com/CocoaPods/Specs.git'
use_frameworks!

def shared_pods
  pod 'Unbox'
  pod 'Promis'
  pod 'Tiguer', :git => 'https://github.com/bmctigue/Tiguer.git'
end

target 'Students' do
  shared_pods
end

target 'StudentsTests' do
  shared_pods
end
